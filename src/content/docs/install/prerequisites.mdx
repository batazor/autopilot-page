---
title: Prerequisites
description: Required tools and BlueStacks setup before running the Autopilot stack. Per-platform requirements for macOS, Linux, and Windows.
sidebar:
  order: 1
---

import { Tabs, TabItem, LinkCard, CardGrid } from '@astrojs/starlight/components';

:::tip
Pre-built images on GitHub Container Registry — no Python / uv install needed.
:::

## Required software

<Tabs syncKey="os">
  <TabItem label="macOS">
    <CardGrid>
      <LinkCard
        title="Docker Desktop"
        description="latest — enable Settings → Resources → Network → host networking (beta), or the bot can't reach the emulator. No host adb needed — the bot ships its own."
        href="https://docs.docker.com/desktop/install/mac-install/"
      />
      <LinkCard
        title="BlueStacks 5+"
        description="Apple-silicon and Intel both supported. Enable ADB in its settings."
        href="https://www.bluestacks.com/"
      />
    </CardGrid>
  </TabItem>

  <TabItem label="Linux">
    <CardGrid>
      <LinkCard
        title="Docker Engine + Compose v2"
        description="latest — network_mode: host works out of the box, no extra toggles. The bot ships its own adb, so none on the host."
        href="https://docs.docker.com/engine/install/"
      />
      <LinkCard
        title="BlueStacks 5+"
        description="Or any 720 × 1280 / 320 DPI Android emulator with ADB enabled."
        href="https://www.bluestacks.com/"
      />
    </CardGrid>
  </TabItem>

  <TabItem label="Windows">
    <CardGrid>
      <LinkCard
        title="Docker Desktop"
        description="latest — requires WSL2; enable Settings → Resources → Network → host networking (beta). No host adb needed — the bot ships its own."
        href="https://docs.docker.com/desktop/install/windows-install/"
      />
      <LinkCard
        title="BlueStacks 5+"
        description="Settings → Advanced → Android Debug Bridge → Enabled."
        href="https://www.bluestacks.com/"
      />
    </CardGrid>
  </TabItem>
</Tabs>

:::caution[Emulator config]
The emulator must run **720 × 1280, 320 DPI, English game language**. See [Emulator setup](/config/emulator/) for the full settings list.
:::

## Get the compose file

The autopilot bot is distributed as Docker images. You only need [`docker-compose.prod.yml`](/docker-compose.prod.yml) to run it:

```sh
curl -fsSL https://batazor.github.io/autopilot-page/docker-compose.prod.yml -o docker-compose.prod.yml
```

Or save it manually from the link above.

:::tip[On Windows, skip this]
Windows users don't fetch the compose file by hand — the [**`install.bat`**](/install.bat)
installer downloads it and starts the stack on a double-click. See
[Install on Windows](/install/windows/).
:::

## Next: install on your platform

<CardGrid>
  <LinkCard title="Install on macOS" href="/install/macos/" />
  <LinkCard title="Install on Linux" href="/install/linux/" />
  <LinkCard title="Install on Windows" href="/install/windows/" />
</CardGrid>
